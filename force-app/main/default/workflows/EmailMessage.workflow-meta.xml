<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <rules>
        <fullName>ReOpen Closed Case on new email</fullName>
        <actions>
            <name>change_status_to_re_opened</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <description>if a case is closed but a new email is received, re-open the case.</description>
        <formula>AND(Incoming = true,  Parent.IsClosed = true,  Parent.RecordType.Name  &lt;&gt; &apos;Care Comm&apos;)</formula>
        <triggerType>onCreateOrTriggeringUpdate</triggerType>
    </rules>
</Workflow>
