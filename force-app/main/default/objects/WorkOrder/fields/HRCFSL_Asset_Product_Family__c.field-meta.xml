<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>HRCFSL_Asset_Product_Family__c</fullName>
    <description>Product Family from the associated Asset. Needed to be sent to JDE via work order integration. HF-67.</description>
    <externalId>false</externalId>
    <formula>IF(NOT( ISBLANK(TEXT(HRCSV_Product__r.Family) ) ),Text(HRCSV_Product__r.Family) ,
IF(NOT( ISBLANK(Asset.HRCFSL_Real_Product_Family__c) ),Asset.HRCFSL_Real_Product_Family__c,  HRCFSL_Product_Family__c ))</formula>
    <label>Product Family</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
