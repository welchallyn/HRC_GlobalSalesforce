<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>HRCSVC_Status_Notes__c</fullName>
    <externalId>false</externalId>
    <formula>IF( HRCSVC_Active_Status__c , &quot;Active&quot; , 

IF( ServiceContract.HRCSVC_ERP_Status_New__c &lt;&gt; &quot;Active&quot;,&quot;Service Contract is not active (Status = &quot; &amp;  TEXT(ServiceContract.HRCSVC_ERP_Status__c) &amp; &quot; )&quot;,
IF(NOT(ISBLANK( ContractLineItem.HRCSVC_Cancel_Date__c )),&quot;Contract Line Item is CANCELED&quot;,
IF(NOT(ISBLANK(ContractLineItem.HRCSVC_Suspend_Date__c )),&quot;Contract Line Item is SUSPENDED&quot;,&quot;&quot;
)

)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Status Notes</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
